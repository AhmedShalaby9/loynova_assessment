name: shopplus_api_client
description: A reusable HTTP client package with interceptors, retry logic, and error handling for ShopPlus apps.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.24.0
  mocktail: ^1.0.0
